# bugs
* check if some of daophot actions need the daophot.opt (such as substar)
* configure to make the *.inc hardcoded parameters

# features
* calibration/daogrow
* iraf i/o
* automate daomatch
* rewrite shorter help
* more recipes
